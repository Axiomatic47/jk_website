{
  "$comment": "Read by the OurStudio SITES preview mode (spec: agent_notes/studio-spec/20260914_sites_preview_mode_spec.md; contract agreed website-developer f28bb754 <-> studio-spec a62261ad, 2026-09-15). The Studio launches `dev` or `static` from this repo root and frames http://localhost:<port><ready_path> once it answers 200. Ports are pinned so the three sites never collide: jk 3100/3101, ink 3200/3201, loe 3300/3301.",
  "name": "kirchner.cv",
  "dev": {
    "argv": [
      "npx",
      "next",
      "dev",
      "-p",
      "3100"
    ],
    "port": 3100,
    "ready_path": "/"
  },
  "static": {
    "build_argv": [
      "npm",
      "run",
      "build"
    ],
    "serve_argv": [
      "npx",
      "serve",
      "out",
      "-l",
      "3101",
      "--no-clipboard"
    ],
    "port": 3101,
    "ready_path": "/"
  },
  "env": {
    "BROWSER": "none",
    "NEXT_TELEMETRY_DISABLED": "1"
  },
  "notes": "Static Next export (output: 'export'). No env file, no backend: every page renders from content/*.json and markdown at build time. Netlify-only features (Open Readings answer form, admin console) post to Netlify functions and are inert locally. No X-Frame-Options or basePath in dev; the SAMEORIGIN/CSP headers in netlify.toml apply only on Netlify. `npx serve out` serves clean URLs (/work/x -> work/x.html) and byte ranges (the PDF viewers range-load). `pages` (additive, read by the Studio's DEVICES run, frontend-developer a168bcf6 2026-09-15): `exclude` = owner console, Netlify functions and the raw per-PDF viewer chrome; `heavy` = pages that fetch a manifest and a PDF (the work/review pages, the archive leaves) and get the run's longer settle.",
  "pages": {
    "exclude": [
      "/admin/*",
      "/api/*",
      "/research/*/doc/*"
    ],
    "heavy": [
      "/work/*",
      "/research/*/leaf/*"
    ]
  }
}
